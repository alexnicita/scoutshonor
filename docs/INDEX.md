# Documentation Index

- **Overview**: See `README.md` for project purpose, quickstart, and layout.
- **Tooling**: Make targets wrap scripts in `scripts/` (setup, run, test, lint, fmt, clean).
- **Configuration**: Copy `.env.example` to `.env`; never commit secrets. `.gitignore` already excludes `.env`.
- **Observability**: Structured logging stub lives at `src/obs/logging.py` for future metrics/tracing hooks.
- **Examples**: API walkthroughs under `examples/` (`recruiter_flow.md`, `sourcing.md`, `descriptions.md`).
- **Next docs**: Add architecture notes, request/response schemas, and operational runbooks here.
